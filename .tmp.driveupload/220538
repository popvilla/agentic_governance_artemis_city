import React from 'react';

interface PanelFrameProps {
  title: string;
  icon?: React.ReactNode;
  children: React.ReactNode;
}

const PanelFrame: React.FC<PanelFrameProps> = ({ title, icon, children }) => {
  return (
    <div className="flex flex-col bg-slate-200/50 dark:bg-slate-800/50 border border-slate-300 dark:border-slate-700 overflow-hidden">
      <div className="flex-shrink-0 p-2 border-b border-slate-300 dark:border-slate-700 text-sm font-semibold text-slate-700 dark:text-slate-300 flex items-center gap-2">
        {icon}
        <span>{title}</span>
      </div>
      <div className="flex-grow overflow-hidden">
        {children}
      </div>
    </div>
  );
};

export default PanelFrame;