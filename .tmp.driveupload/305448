"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.searchReplace = searchReplace;
const methods_1 = require("../../services/obsidianRestAPI/methods");
const logger_1 = require("../../utils/logger");
async function searchReplace(path, search, replace) {
    try {
        logger_1.logger.debug(`Performing search and replace in note: ${path}, search: '${search}'`);
        const updatedContent = await (0, methods_1.searchReplace)(path, search, replace);
        logger_1.logger.info(`Successfully performed search and replace in note: ${path}`);
        return { success: true, data: { path, content: updatedContent }, message: `Search and replace in '${path}' successful.` };
    }
    catch (error) {
        logger_1.logger.error(`Error during search and replace in note '${path}': ${error.message}`);
        return { success: false, error: error.message };
    }
}
//# sourceMappingURL=obsidianSearchReplaceTool.js.map